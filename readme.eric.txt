
$ vagrant up

$ vagrant ssh

$ cd /src/ats

$ autoreconf -if

$ ./configure

$ make

$ sudo make install

$ sudo /src/ats/trafficserver /etc/init.d/

$ ldconfig /usr/local/lib

$ sudo /etc/init.d/trafficserver start
